# Built (arch-specific) FM helper binary — rebuilt by install.sh / build-fm-helper.mjs
assets/fm-helper/wtcp-fm-helper
*.tmp
.DS_Store

# workmux runtime hooks (leaked during testing)
.github/hooks/
.workmux/
